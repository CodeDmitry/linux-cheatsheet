bash history is stored in `~/.bash_history`.

it usually contains the previous 500 lines by default.

You can set HISTSIZE, HISTIGNORE, HISTFILESIZE variables
    to control various aspects of `.bash_history`.
